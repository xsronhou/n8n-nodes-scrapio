// n8n community nodes are loaded via the "n8n" field in package.json
// (dist/credentials/*.js, dist/nodes/**/*.js); this file exists only so the
// package has a conventional entry point if something resolves it directly.
module.exports = {};
